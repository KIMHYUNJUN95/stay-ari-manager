import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
console.log("%c HARU DASHBOARD v2026.01.27.03 (RESTORED) ", "background: #222; color: #bada55; font-size: 20px");
root.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
