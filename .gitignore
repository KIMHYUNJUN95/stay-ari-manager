# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Windows null device files
nul
**/nul
build/
all_keys_found.txt

# Firebase Service Account Key (NEVER COMMIT!)
serviceAccountKey.json

# Env / secrets (NEVER COMMIT!)
.env
.env.*
!.env.example

# Firebase Functions params (웹훅 URL 등)
functions/.env
functions/.env.*

# npm cache
.npm-cache/

# 로컬 실행 산출물 / 임시 파일 (자격증명 포함 가능 — 커밋 금지)
.firebase-adc-profile/
tmp/
output/
outputs/
exports/
